import React from "react";
import ReactDOM from "react-dom/client";

import { DesignPreviewApp } from "./preview-app";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <DesignPreviewApp />
  </React.StrictMode>,
);
